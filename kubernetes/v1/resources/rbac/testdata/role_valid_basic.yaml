metadata:
  name: pod-reader
  namespace: default
  labels:
    app: my-app
rules:
  - apiGroups:
      - ""
    resources:
      - pods
      - pods/log
    verbs:
      - get
      - list
      - watch
